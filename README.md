## Frontend For Car Export Company

This frontend makes calls to the backend API written in Django for Authentication, Retrieving Stock at the moment.

- **Auth Context**: Handles all account related features.
- **Car Context**: Handles cars logic by setting selected cars and search results to context.